$(document).on('ready', function() {

    var outerShell = $('<div>', {
        class: 'outer-shell',
    })


    var innerShell = $('<div>', {
        class: 'inner-shell',
    })

    var innerShell = $('<div>', {
        class: 'inner-shell',
    })




    $('.clock-container').append(outerShell.append(innerShell));

});